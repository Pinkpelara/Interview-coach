version: "3.9"

services:
  gpu-workers:
    build:
      context: ./services/gpu-workers
      dockerfile: Dockerfile
    environment:
      CUDA_VISIBLE_DEVICES: all
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
